#include <fstream>
#include <cstdio>
#include <cstring>

using namespace std;

int main(){
    ifstream fin("data.in");
    ofstream fout("data.txt");
    
    string mes;
    int num = 1;
    
    while(getline(fin,mes)){
    	string a = mes.substr(0,3);
	}
    
    return 0;
}
